# fd-cpn — 쿠폰관리

  Family Diary 실습 — 쿠폰관리 도메인 설계 레포 (담당 영역 C).

  - 설계 파일: `model/` 안 `.sl` (axse-design DSL)
  - 산출물: `out/` (Spring Boot · PlantUML 자동 생성, gitignore)
  - 짝 개발 레포: `fd-cpn-service` (axse-dev-ws)
